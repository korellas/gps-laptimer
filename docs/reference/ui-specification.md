# UI 사양

**상태:** 2026-02-15 기준 최신
**SSOT:** UI 레이아웃 및 시각 디자인의 단일 정보원

## 1. 디스플레이 구성

| 매개변수 | 값 |
|----------|-----|
| **해상도** | 640×172 픽셀 (가로) |
| **색 심도** | RGB565 (16비트) |
| **UI 프레임워크** | LVGL 9.x |
| **업데이트 속도** | 30 Hz (33ms 간격) |
| **방향** | 가로 (패널 네이티브에서 90° 회전) |

## 2. 전체 레이아웃

```
┌───────────────────────────────────────────────────────────────────┐
│ [🛰▮▮▮▯▯▯▯▯▯▯]         12:34 PM         [🔋██████]             │  ← Status Bar (y=0~16)
│  S1 -0.42              LAP 01                   1:23.4           │  ← 상단 행 (y=22~)
│  S2 +0.72                                     BEST 1:27:00       │  ← 두 번째 행
│  S3 -0.32               +1.34                                    │  ← 중앙 (델타)
│  ███████████████████████████████                      +1.2km/h    │  ← 하단 (바)
└───────────────────────────────────────────────────────────────────┘
  ↑                      ↑                         ↑
  좌측 컬럼              중앙 컬럼                 우측 컬럼
  (GPS/섹터)             (시계/델타)              (배터리/랩타임/베스트)
```

## 3. UI 요소

### 3.0 Status Bar (y=0~16)

| 요소 | 위치 | 크기 | 설명 |
|------|------|------|------|
| **GPS 위성 아이콘** | 좌상단 (4, 2) | 12×12 | 위성 모양 (본체 + 태양전지판) |
| **GPS 신호 바** | 아이콘 오른쪽 | 2px×(4~12)px × 10개 | 활성=white 채움, 비활성=outline |
| **GPS "+" 표시** | 바 오른쪽 | 6×6 | >10 위성 시 표시 (lv_obj cross) |
| **시계** | 상단 중앙 | montserrat_14 | `h:mm AM/PM` (KST, 12시간제) |
| **배터리 아이콘** | 우상단 (-8, 2) | 36×12 | 라운드 사각형 (radius=3), white 외곽 |
| **배터리 Fill** | 아이콘 내부 | max 32×8 | green (#14AA14), <20% → red (#AA1414) |

**GPS 신호 바 규칙:**
- 바 1개 = 위성 1개, 최대 10개 표시
- >10 위성: 10개 모두 채움 + "+" 아이콘
- 높이: 좌→우 점진적 증가 (4px → 12px)
- 비활성 바: #666666 외곽선만 (빈 사각형)

**시계 규칙:**
- GPS 시간 동기화 전: `--:--` 표시
- GPS 시간 수신 후: settimeofday()로 시스템 시계 설정, KST(UTC+9) 표시
- 폰트: montserrat_14, 색상: #888888

### 3.1 상단 행
| 요소 | 위치 | 폰트 | 형식 | 예시 |
|------|------|------|------|------|
| **랩 번호** | 상단 중앙 | - | `LAP ##` | `LAP 01` |
| **실시간 랩 타임** | 상단 우측 | 큼 | `m:ss.d` | `1:23.4` |

### 3.2 두 번째 행
| 요소 | 위치 | 폰트 | 형식 | 예시 |
|------|------|------|------|------|
| **베스트 랩 타임** | 상단 우측 (랩 타임 아래) | 중간 | `BEST m:ss:cc` | `BEST 1:27:00` |

### 3.3 중앙 영역
| 요소 | 위치 | 폰트 | 색상 | 형식 | 예시 |
|------|------|------|------|------|------|
| **델타 시간** | 중앙 | 매우 큼 | 흰색 | `±#.##` | `+1.34` |

**델타 시간 규칙:**
- 부호: 항상 `+` 또는 `-` 표시
- 정밀도: 소수점 2자리
- 선행 0 없음 (예: `+0.42`, `+00.42` 아님)
- 색상: 항상 흰색 (색상 코딩 없음)
- 의미: `+` = 레퍼런스보다 느림, `-` = 빠름

### 3.4 좌측 컬럼 (섹터 델타)
| 요소 | 위치 | 폰트 | 색상 | 형식 | 예시 |
|------|------|------|------|------|------|
| **S1 델타** | 좌측, 1행 | Share Tech Mono 24pt | 흰색 | `S1 ±#.##` | `S1 -0.42` |
| **S2 델타** | 좌측, 2행 | Share Tech Mono 24pt | 흰색 | `S2 ±#.##` | `S2 +0.72` |
| **S3 델타** | 좌측, 3행 | Share Tech Mono 24pt | 흰색 | `S3 ±#.##` | `S3 -0.32` |

**섹터 델타 표시 규칙:**
- **완료된 섹터:** `sectorDeltas[i]`의 고정 값 표시
- **현재 섹터:** 실시간 델타 표시 (총 델타 - 완료된 누적)
- **미래 섹터:** 숨김 (표시 안 함)
- **색상:** 항상 흰색 (녹색/빨간색 코딩 없음)
- **정밀도:** 소수점 2자리
- **누적 속성:** 모든 섹터 델타 합 = 총 랩 델타 (±0.01 반올림)

### 3.5 하단 바 (속도 델타)
| 요소 | 유형 | 색상 (빠를 때) | 색상 (느릴 때) |
|------|------|--------------|--------------|
| **배경 바** | 전체 너비 프로그레스 바 | 녹색 `#14AA14` | 빨강 `#AA1414` |
| **속도 델타 텍스트** | 바 가장자리에 떠 있음 | 녹색 `#14AA14` | 빨강 `#AA1414` |

**바 구성:**
- 너비: 전체 화면 (640px)
- 높이: ~30px
- 중심점: 50% (0 델타)
- 범위: ±2초 (시간 델타)
- 색상: 빠를 때 녹색 (음수 델타), 느릴 때 빨강 (양수 델타)

**속도 델타 텍스트:**
- 형식: `±#.#km/h`
- 위치: 바 가장자리에 동적 배치 (바와 함께 이동)
- 예시: `+1.2km/h` (느림) 또는 `-3.4km/h` (빠름)
- 색상: 바 색상과 일치

## 4. 색상 팔레트

| 색상 이름 | 16진수 코드 | RGB | 사용 용도 |
|----------|-----------|-----|----------|
| **녹색 (빠름)** | `#14AA14` | (20, 170, 20) | 앞서 있을 때 바 + 속도 텍스트 |
| **빨강 (느림)** | `#AA1414` | (170, 20, 20) | 뒤처져 있을 때 바 + 속도 텍스트 |
| **흰색** | `#FFFFFF` | (255, 255, 255) | 델타 시간, 섹터 델타, 랩 타임 |
| **검정** | `#000000` | (0, 0, 0) | 배경 |

**색상 코딩 규칙:**
- **시간 델타 (중앙):** 항상 흰색
- **섹터 델타 (좌측):** 항상 흰색
- **속도 바/텍스트 (하단):** 녹색 (빠름) 또는 빨강 (느림)

## 5. 폰트

| 요소 | 폰트 | 크기 | 스타일 |
|------|------|------|--------|
| 섹터 델타 | Share Tech Mono | 24pt | Regular |
| 델타 시간 | 기본값 | 72pt | Bold |
| 랩 타임 | 기본값 | 48pt | Regular |
| 베스트 타임 | 기본값 | 24pt | Regular |
| 속도 델타 | 기본값 | 20pt | Regular |

**폰트 파일:**
- Share Tech Mono: `main/fonts/ShareTechMono-Regular.ttf`
- 기본값: LVGL 내장 폰트

## 6. 레이아웃 치수 (대략)

```
┌────────────────────────────────────────────────────────────────┐
│ 0,0                                                    640,0  │
│  ┌──────────┐         ┌──────────┐         ┌──────────┐      │
│  │ Sector   │         │ LAP ##   │         │ 1:23.4   │  40px│
│  │ S1 ±#.## │         │          │         │          │      │
│  ├──────────┤         └──────────┘         ├──────────┤      │
│  │ S2 ±#.## │                              │ BEST ... │  80px│
│  ├──────────┤                              └──────────┘      │
│  │ S3 ±#.## │         ┌──────────┐                           │
│  └──────────┘         │  ±#.##   │                       120px│
│   120px wide          │ (delta)  │          300px wide       │
│                       └──────────┘                           │
│                        200px wide                            │
│  ┌──────────────────────────────────────────────────────┐   │
│  │████████████████████│                        ±#.#km/h │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                          172px│
│ 0,172                                                640,172 │
└────────────────────────────────────────────────────────────────┘
```

## 7. 애니메이션 및 전환

| 요소 | 애니메이션 | 지속 시간 |
|------|-----------|----------|
| 델타 시간 | 부드러운 업데이트 (EMA 스무딩) | - |
| 속도 바 | 부드러운 슬라이드 | - |
| 섹터 델타 | 섹터 완료 시 즉시 업데이트 | - |
| 랩 완료 | 오버레이 플래시 | ~2초 |

**스무딩:**
- 시간 델타: EMA alpha = 0.15
- 속도 델타: EMA alpha = 0.20

## 8. 특수 상태

### 8.1 레퍼런스 랩 없음
| 요소 | 표시 |
|------|------|
| 델타 시간 | `--` (대시) |
| 속도 바 | 숨김 또는 중립 |
| 섹터 델타 | 숨김 |

### 8.2 GPS 신호 손실
| 요소 | 표시 |
|------|------|
| 상태 표시기 | `GPS LOST` (깜박임) |
| 델타 시간 | 마지막으로 알려진 값 (고정) |

### 8.3 랩 완료 오버레이
```
┌─────────────────────────────────────┐
│                                     │
│         LAP COMPLETE!               │
│                                     │
│         Time: 1:23.45               │
│         Delta: +1.23                │
│                                     │
└─────────────────────────────────────┘
```
- 표시: 2초
- 배경: 반투명 오버레이
- 자동 닫힘: 일반 뷰로 복귀

### 8.4 새로운 베스트 랩
```
┌─────────────────────────────────────┐
│                                     │
│       🏆 NEW BEST LAP! 🏆          │
│                                     │
│         Time: 1:19.87               │
│                                     │
└─────────────────────────────────────┘
```
- 표시: 3초
- 색상: 녹색 배경
- 자동 닫힘: 일반 뷰로 복귀

## 9. 업데이트 전략

| 요소 | 업데이트 빈도 | 트리거 |
|------|-------------|--------|
| 랩 타임 | 10 Hz (100ms) | 실시간 타이머 |
| 델타 시간 | 10 Hz | 새 GPS 포인트 / 시뮬레이션 업데이트 |
| 섹터 델타 (현재) | 10 Hz | 실시간 계산 |
| 섹터 델타 (완료) | 이벤트 발생 시 | 섹터 경계 통과 |
| 속도 바 | 10 Hz | 속도 델타 업데이트 |
| 디스플레이 리프레시 | 30 Hz | LVGL 타이머 (33ms) |

## 10. 디스플레이 데이터 구조

LVGL 렌더링 전에 별도의 프레임 버퍼에 디스플레이 데이터 준비:

```cpp
// main/waveshare_display.cpp
static char lFrame[32];   // 랩 타임 프레임
static char gFrame[32];   // GPS/델타 프레임
static char tFrame[32];   // 시간 프레임
```

`main/main.cpp`의 `updateDisplayData()`가 채우고, `displayLoop()`에서 LVGL이 렌더링.

## 11. 구현 참고사항

### 11.1 플러시 파이프라인
```
LVGL 렌더
  └→ RGB565 버퍼
     └→ 바이트 스왑 (RGB565 형식 변환)
        └→ 소프트웨어 회전 (90° 가로로)
           └→ DMA 청크 업로드 (AXS15231B로)
              └→ on_color_trans_done 콜백
                 └→ 세마포어 (다음 플러시 페이싱)
```

### 11.2 성능 고려사항
- LVGL 틱: 16ms (LVGL_TICK_PERIOD_MS)
- 디스플레이 루프: 33ms (30 Hz)
- 렌더 경로에서 블로킹 작업 피하기
- 큰 전송에는 DMA 사용
- 세마포어를 통한 플러시 동기화

## 참조

- **하드웨어 사양:** [hardware.md](hardware.md)
- **아키텍처:** [architecture.md](architecture.md)
- **데이터 구조:** [data-structures.md](data-structures.md)
- **폰트 파일:** `main/fonts/ShareTechMono-Regular.ttf`
